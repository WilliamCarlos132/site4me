# 核心：排除node_modules依赖包
node_modules/
# 排除IDE的配置文件（如IDEA的.idea）
.idea/
# 排除项目打包后的构建文件
dist/
build/
# 排除环境变量配置文件（如有）
.env
.env.*
# 排除日志文件
*.log
# 排除npm的缓存文件
package-lock.json
yarn.lock
# 排除Windows的系统隐藏文件
Thumbs.db
.DS_Store